/* Community links — single source. Founder fills the handles; until then
   the landing/footer render NOTHING (no fake socials — the honesty law
   applies to community presence too). */
export const COMMUNITY: { label: string; url: string }[] = [
  // { label: 'Telegram', url: 'https://t.me/...' },   // ← isi saat grup dibuat
  // { label: 'X', url: 'https://x.com/...' },         // ← isi saat akun dibuat
]
